# TinaCMS — optional. Empty values keep Tina in local mode, which is fine
# for solo editing on your dev machine. Set these only when you want cloud-
# hosted editing (signup at app.tina.io, free up to 2 editors).
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=
