# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# tinacms
# Keep schema artifacts (_graphql.json, _lookup.json, _schema.json, client.ts,
# types.ts, *.gql, config.prebuild.jsx) so CI can build without re-running
# tinacms dev. Ignore only the local search-index DB and Tina-build's HTML output.
/tina/__generated__/db
/tina/__generated__/.cache
/tina/tina-lock.json.bak
/public/admin
