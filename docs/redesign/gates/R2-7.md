# Gates R2-7 — GEO/SEO-Paket
- [x] G1 llms.txt: Vollausbau (Leistungen, Zielgruppen, Zahlen, Vergleiche, R2-Seiten), Unternehmensberatung
      CHECK: grep -c "Unternehmensberatung\|17 Jahr" src/app/llms.txt/route.ts
      EXPECT: >=1
      EVIDENCE: `6` — H1 „beuwy — Unternehmensberatung für Immobilienunternehmen",
      Blockquote + Positionierung nennen „Unternehmensberatung" und „17 Jahre"
      wiederholt; volle H2-Gliederung (Positionierung, Leistungen, Seiten inkl.
      aller 5 R2-Routen, Referenzen, Vergleich BOTTIMMO, Kosten-Spannen,
      Kontakt). Keine Selbstbezeichnung als "Agentur":
      `grep -n "Agentur" src/app/llms.txt/route.ts` → 5 Treffer, alle entweder
      Negation ("keine Agentur") oder Fremdbezeichnung (Konkurrenz-Kategorie /
      Kosten-Stufen "Template-Agentur"/"individuelle Agentur", wörtlich aus
      /maklerwebsite-kosten übernommen) — nie beuwy selbst.
- [x] G2 JSON-LD: Organization+Service im Layout, FAQPage auf FAQ-Seiten (valide, keine erfundenen Ratings)
      CHECK: grep -c "OrganisationLd" src/app/layout.tsx && grep -c "ProfessionalService\|\"@type\": \"Service\"" src/components/SchemaOrg.tsx
      EXPECT: >=1 je Check
      EVIDENCE: `2` (Import + Rendering in layout.tsx, einmal global im body) /
      `4` (ProfessionalService in OrganisationLd, Service in ServiceLd).
      `OrganisationLd`: name, founder (Alexander Pütter), address (Max-Bill-Str.
      3, 67061 Ludwigshafen, aus Impressum), areaServed, url, sameAs, email —
      keine Ratings/Reviews/Telefonnummern erfunden. FAQPage-JSON-LD liegt
      bereits inline auf 10 Seiten (`grep -rlc "FAQPage" src/app --include
      page.tsx | grep -v :0 | wc -l` → 10, u.a. alle 5 R2-Seiten), gebaut von
      den jeweiligen Sibling-Leaves — außerhalb der Dateiliste dieses Leafs.
      `ServiceLd` steht bereit, ist aber in keiner page.tsx verdrahtet (siehe
      GEO-STRATEGIE.md "Bekannte Lücken").
- [x] G3 sitemap + robots um R2-Seiten; Footer/Hub verlinken sie
      CHECK: grep -c "ki-fuer-immobilienmakler" src/app/sitemap.ts
      EXPECT: >=1
      EVIDENCE: `1` (und alle 5 R2-Routen: `grep -c
      "ki-fuer-immobilienmakler\|immobilienmarketing-agentur\|marketing-projektentwickler\|marketing-bautraeger\|marketing-immobilienvertrieb"
      src/app/sitemap.ts` → 5, Prioritäten 0.8/0.75 gestaffelt zwischen Hub und
      Cluster). robots.ts geprüft, unverändert: `/anfrage` bleibt disallowed,
      kein neuer Blockbedarf. Footer/Hub verlinken die R2-Seiten bereits
      (Sibling-Leaves, außerhalb der Dateiliste): `grep -c
      "ki-fuer-immobilienmakler\|...\|marketing-immobilienvertrieb"
      src/components/Footer.tsx` und `.../immobilienmarketing/page.tsx` je >=1.
- [x] G4 Jede indexierte Seite beantwortet ihre Suchfrage im ersten Absatz woertlich (Stichprobe 4 Seiten dokumentieren)
      CHECK: ls docs/redesign/GEO-STRATEGIE.md | wc -l
      EXPECT: 1
      EVIDENCE: `1` — Stichprobe (`/leadgenerierung-immobilienmakler`,
      `/bottimmo-alternative`, `/ki-fuer-immobilienmakler`,
      `/immobilienmarketing-agentur`) mit H1/Erstabsatz-Zitaten dokumentiert
      in GEO-STRATEGIE.md § G4, alle vier bestehen.
