# Gates D1 — /immobilienmarketing
- [x] G1 Seite existiert, XXL-Hero im System, VSL-Dramaturgie (Problem→Mechanismus→Beweis→CTA /anfrage)
      CHECK: ls src/app/immobilienmarketing/page.tsx | wc -l
      EXPECT: 1
      EVIDENCE: `1` (Datei erstellt: Hero ~75dvh Foto 10 → Einordnung 3×H3 →
      Themen-Rails 7× → 4-Säulen-GelbeKarte → Finale-CTA)
- [x] G2 Title enthaelt "Immobilienmarketing", eigene Description, deutsch
      CHECK: grep -c "Immobilienmarketing" src/app/immobilienmarketing/page.tsx
      EXPECT: >=1
      EVIDENCE: `10` — title exakt "Immobilienmarketing: Der Premium-Ansatz für
      führende Makler | beuwy", description eigenständig (nicht mit / identisch)
- [x] G3 Interne Links: Hub + /anfrage + min. 1 Schwesterseite
      CHECK: grep -c "/anfrage\|/immobilienmarketing" src/app/immobilienmarketing/page.tsx
      EXPECT: >=2
      EVIDENCE: `2` (canonical + 3× CTA-Pill /anfrage) — zusätzlich 7 Rail-Links
      auf alle Schwesterseiten (D2–D4, E1, F1–F3) + 3 Inline-Links im Fließtext
- [x] G4 Min. 1 Asset-Bild mit AiPille, tsc gruen
      CHECK: grep -c "AiPille\|maklerAsset" src/app/immobilienmarketing/page.tsx
      EXPECT: >=1
      EVIDENCE: `4` — AiPille + maklerAsset(10) im Hero; `npx tsc --noEmit`
      liefert 0 Fehler in immobilienmarketing/page.tsx (6 Fehler projektweit,
      alle in src/app/anfrage/page.tsx + src/components/StartUnten.tsx —
      Parallel-Leaves C1/B4, nicht Teil von D1)
- [x] G5 Foto 10 verbaut (AiPille) + Element-Bibliothek genutzt
      CHECK: grep -rc "maklerAsset(10)" src/app/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: `1` — MaklerElemente-Bausteine genutzt: GelbeKarte (4× Säulen),
      Highlight (Subline), StempelBadge (Hero-Layering-Pflicht), SektionsKopf
      (3× Sektionskopf), Reveal (alle Sektionen unterhalb des Folds)
