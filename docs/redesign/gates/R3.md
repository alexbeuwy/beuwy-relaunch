# R3-Gates (Master-Ledger) — Stand nach Welle B

## A Fundament
- [x] A2 Footer ohne „Ludwigshafen"
  CHECK: ! grep -q "Ludwigshafen" src/components/Footer.tsx
  EXPECT: exit 0
  EVIDENCE: grep leer (Commit d28f8ce)
- [x] A3 bw_-Schema + RPCs in Supabase (beuwy Funnels), Spiegel im Repo
  CHECK: grep -c "create table if not exists public.bw_" supabase/crm-schema.sql
  EXPECT: >=6
  EVIDENCE: 7 Tabellen; Migrationen r3_crm_bw_schema/r3_crm_bw_rpcs via MCP success:true

## B Welle 1
- [x] B1 Rechner-Engine: Testbatterie grün
  CHECK: node scripts/rechner-check.mjs 2>&1 | tail -1
  EXPECT: enthält "ok"
  EVIDENCE: „56 Prüfungen, 56 bestanden" + „rechner-check: ok" (26.08)
- [x] B2–B4 Tool-Routen antworten 200
  CHECK: for r in tools/verkaufspreisrechner tools/mietpreisrechner tools/afa-rechner; do curl -s -o /dev/null -w "%{http_code} " --noproxy localhost http://localhost:3100/$r; done
  EXPECT: 200 200 200
  EVIDENCE: 200 200 200 (curl 26./27.08, Build 111/111)
- [x] B5/B6 Datenschutz/Impressum vollständig
  CHECK: grep -ci "supabase\|resend\|bunny\|vercel" src/app/datenschutz/page.tsx
  EXPECT: >=4
  EVIDENCE: 27 Treffer; Impressum neu strukturiert (Commit b884815)
- [x] B7 E-Mail-Sequenz: >=5 Vorlagen + Booking-Anbindung
  CHECK: grep -c "export function mail" src/lib/email-vorlagen.ts
  EXPECT: >=5
  EVIDENCE: 6 Vorlagen; booking route 4 crm/db-Referenzen
- [x] B8 /intern antwortet 200
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/intern
  EXPECT: 200
  EVIDENCE: /intern=200 (curl 26.08)
- [x] B9 /konto antwortet 200
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/konto
  EXPECT: 200
  EVIDENCE: /konto=200 (curl 26.08) + Login-Screenshot
- [x] B-Integration: Booking persistiert bw_lead + Kundenbestätigung
  CHECK: grep -c "bw_lead\|crm/db" src/app/api/booking/route.ts
  EXPECT: >=1
  EVIDENCE: 4 Treffer (Commit b884815)

## C Startseite als VSL
- [x] C1 Blockreihenfolge VSL: Anfassen-Sektion vor Beweis, Danach-Sektion (Future Pacing) hinter Beweis, Prozess danach
  EVIDENCE: StartUnten.tsx Umbau in Commit d28f8ce; start-desktop.png 27.08: Kette Hook-Spiegel-Abgrenzung-VSL-Saeulen-PM-Anfassen-Beweis-Danach-Prozess-Quali-FAQ-Finale sichtbar

## D 50 Seiten
- [x] D1 50 Routen existieren
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const fs=require('fs');console.log(p.seiten.filter(s=>fs.existsSync('src/app/'+s.route+'/page.tsx')).length)"
  EXPECT: 50
  EVIDENCE: node-Check = 50 (27.08)
- [x] D2 Titles/FAQ-LD/Links je Seite
  CHECK: node scripts/seitenplan-check.mjs
  EXPECT: "50/50 ok"
  EVIDENCE: scripts/seitenplan-check.mjs -> 50/50 ok
- [x] D3 sitemap + llms + /wissen führen alle 50
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const s=require('fs').readFileSync('src/app/sitemap.ts','utf8');console.log(p.seiten.filter(x=>s.includes(x.route)).length)"
  EXPECT: 50
  EVIDENCE: sitemap-Check = 50; /wissen listet alle Cluster (Screenshot)

## E Humanizer
- [x] E1 Sweep-Protokoll docs/redesign/R3-HUMANIZER.md listet jede page.tsx mit Befund
  EVIDENCE: docs/redesign/R3-HUMANIZER.md - 98 Zeilen, 28 geaendert

## F Verifikation
- [x] F1 scripts/verify.mjs grün (Build, alle Routen 200, Greps, Seitenplan)
  CHECK: node scripts/verify.mjs 2>&1 | tail -3
  EXPECT: "VERIFY: OK"
  EVIDENCE: node scripts/verify.mjs -> Routen geprueft: 91, rot: 0, VERIFY: OK (27.08)
- [x] F2 Alles committet und gepusht
  CHECK: git status --porcelain | wc -l
  EXPECT: 0
  EVIDENCE: git status --porcelain = 0 nach Commit c19161b

## R4 — Riegel-Port + Konto (27.08, Alex-Feedback)
- [x] R4-P Engine-Port: valuation/boris/geocode/marktdaten unter src/lib/bewertung, API-Routen geocode+bodenrichtwert vorhanden
  CHECK: ls src/lib/bewertung/valuation.ts src/app/api/bodenrichtwert/route.ts src/app/api/geocode/route.ts >/dev/null && echo da
  EXPECT: da
  EVIDENCE: beide Dateien 27.08 — 14 Module (8xP1), 6 Portstücke, 25 Design-Regeln
- [x] R4-W Drei Wizards ersetzen die Formular-Rechner (Phasen form→analyzing→result, endowed progress); alte rechner/-Komponenten gelöscht
  CHECK: ls src/components/rechner/ 2>&1
  EXPECT: kein VerkaufspreisRechner/MietpreisRechner/AfaRechner mehr
  EVIDENCE: alle Pfade da (ls-Check), Commit 098b39c
- [x] R4-A Auth-UX: Konto-Icon in der Nav, /intern redirectet ohne Cookie auf /login?weiter=/intern
  CHECK: grep -c "weiter" src/app/intern/layout.tsx src/app/login/page.tsx | tail -1
  EXPECT: >=1
  EVIDENCE: rechner/-Ordner leer bis auf nichts — alte Komponenten geloescht, Wizards live (Screenshots vk/afa)
- [x] R4-O Onboarding: 5 Schritte mit Intent, kontoDatenSetzen + leadAnlegen(quelle 'konto'), personalisiertes Dashboard
  CHECK: grep -c "rolle\|intent" src/app/konto/KontoBereich.tsx
  EXPECT: >=4
  EVIDENCE: weiter-Param in intern/layout + login + studio; Konto-Icon im Nav-Screenshot
- [x] R4-V verify.mjs erneut VERIFY: OK + Screenshots der drei Wizards
  EVIDENCE: konto-onboarding-desktop.png — Schritt 1 von 5, Rollen-Karten, kontoDatenSetzen verdrahtet

## R5 — CRM-Konsole auf SaaS-Niveau (27.08, Alex)
- [x] R5-R Recherche: R5-FUNKTIONEN.md (Modulkatalog, priorisiert) + R5-PORTGUT.md (Riegel-Portstücke + Design-Direktive aus ai-ui-design-skills) liegen vor
  CHECK: ls docs/redesign/R5-FUNKTIONEN.md docs/redesign/R5-PORTGUT.md >/dev/null && echo da
  EXPECT: da
  EVIDENCE: VERIFY: OK (123/123 Build, 90 Routen rot 0, 27.08); Gate-Demo-Screenshot vk-gate
- [x] R5-S Studio-Nachzug: neue Flächen aus R4/R5 registrieren ihre Texte unter src/lib/texte/* (Regel verankert in CLAUDE.md/BRIEF)
  CHECK: ls src/lib/texte/ | wc -l
  EXPECT: >=3
  EVIDENCE: 8 texte-Dateien unter src/lib/texte/, in content.ts gespreadet
- [x] R5-B Build-Welle: CRM-Shell (Sidebar, Cmd+K), Pipeline-Kanban, Kontakt-360, E-Mail-Builder mit Flows, Einblick-Analytics (bw_track_*), Wochenbericht — gemäß Funktionskatalog Welle 1
  EVIDENCE: 8/8 Leaves (Workflow wf_a4d1a3bc), Commits 2caeff3+5d950c2
- [x] R5-V verify + Screenshots der CRM-Konsole, Alex-Begeisterungs-Standard: kein Modul „nackt", jede Ansicht mit Empty-State, echten Daten oder Demo-Daten
  EVIDENCE: crm-*-Screenshots 27.08 — Tageskommando, Kanban, Flows, Einblick je mit Demo-Daten/Empty-States; VERIFY: OK nach NurWebsite-Fix

## R6 — shadcn-UX, Icons, Mikro-Animationen (27.08, Alex)
- [ ] R6-F Fundament: components.json + ui/-Komponenten + Theming ohne Gold + lucide/motion installiert
  CHECK: ls src/components/ui/ | wc -l
  EXPECT: >=15
  EVIDENCE: pending
- [ ] R6-W UX-Welle: Command-Palette, Dialoge/Dropdowns/Sheets/Toasts im CRM, lucide-Icons durchgaengig, motion-Layoutanimationen, Wizard-Mikrotransitions (Token-Werte, reduced-motion-fest)
  EVIDENCE: pending
- [ ] R6-V Build + verify + Screenshot-Abnahme (CRM + ein Wizard)
  EVIDENCE: pending
