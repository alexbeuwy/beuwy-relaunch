# R3-Gates (Master-Ledger) — Stand nach Welle B

## A Fundament
- [x] A2 Footer ohne „Ludwigshafen"
  CHECK: ! grep -q "Ludwigshafen" src/components/Footer.tsx
  EXPECT: exit 0
  EVIDENCE: grep leer (Commit d28f8ce)
- [x] A3 bw_-Schema + RPCs in Supabase (beuwy Funnels), Spiegel im Repo
  CHECK: grep -c "create table if not exists public.bw_" supabase/crm-schema.sql
  EXPECT: >=6
  EVIDENCE: 7 Tabellen; Migrationen r3_crm_bw_schema/r3_crm_bw_rpcs via MCP success:true

## B Welle 1
- [x] B1 Rechner-Engine: Testbatterie grün
  CHECK: node scripts/rechner-check.mjs 2>&1 | tail -1
  EXPECT: enthält "ok"
  EVIDENCE: „56 Prüfungen, 56 bestanden" + „rechner-check: ok" (26.08)
- [ ] B2–B4 Tool-Routen antworten 200 (nach Gesamt-Build)
  CHECK: for r in tools/verkaufspreisrechner tools/mietpreisrechner tools/afa-rechner; do curl -s -o /dev/null -w "%{http_code} " --noproxy localhost http://localhost:3100/$r; done
  EXPECT: 200 200 200
  EVIDENCE: pending
- [x] B5/B6 Datenschutz/Impressum vollständig
  CHECK: grep -ci "supabase\|resend\|bunny\|vercel" src/app/datenschutz/page.tsx
  EXPECT: >=4
  EVIDENCE: 27 Treffer; Impressum neu strukturiert (Commit b884815)
- [x] B7 E-Mail-Sequenz: >=5 Vorlagen + Booking-Anbindung
  CHECK: grep -c "export function mail" src/lib/email-vorlagen.ts
  EXPECT: >=5
  EVIDENCE: 6 Vorlagen; booking route 4 crm/db-Referenzen
- [ ] B8 /intern antwortet 200 (nach Gesamt-Build)
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/intern
  EXPECT: 200
  EVIDENCE: pending
- [ ] B9 /konto antwortet 200 (nach Gesamt-Build)
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/konto
  EXPECT: 200
  EVIDENCE: pending
- [x] B-Integration: Booking persistiert bw_lead + Kundenbestätigung
  CHECK: grep -c "bw_lead\|crm/db" src/app/api/booking/route.ts
  EXPECT: >=1
  EVIDENCE: 4 Treffer (Commit b884815)

## C Startseite als VSL
- [x] C1 Blockreihenfolge VSL: Anfassen-Sektion vor Beweis, Danach-Sektion (Future Pacing) hinter Beweis, Prozess danach
  EVIDENCE: StartUnten.tsx Umbau in Commit d28f8ce; visuelle Abnahme folgt unter F (Screenshot)

## D 50 Seiten
- [ ] D1 50 Routen existieren
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const fs=require('fs');console.log(p.seiten.filter(s=>fs.existsSync('src/app/'+s.route+'/page.tsx')).length)"
  EXPECT: 50
  EVIDENCE: pending
- [ ] D2 Titles/FAQ-LD/Links je Seite
  CHECK: node scripts/seitenplan-check.mjs
  EXPECT: "50/50 ok"
  EVIDENCE: pending
- [ ] D3 sitemap + llms + /wissen führen alle 50
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const s=require('fs').readFileSync('src/app/sitemap.ts','utf8');console.log(p.seiten.filter(x=>s.includes(x.route)).length)"
  EXPECT: 50
  EVIDENCE: pending

## E Humanizer
- [ ] E1 Sweep-Protokoll docs/redesign/R3-HUMANIZER.md listet jede page.tsx mit Befund
  EVIDENCE: pending

## F Verifikation
- [ ] F1 scripts/verify.mjs grün (Build, alle Routen 200, Greps, Seitenplan)
  CHECK: node scripts/verify.mjs 2>&1 | tail -3
  EXPECT: "VERIFY: OK"
  EVIDENCE: pending
- [ ] F2 Alles committet und gepusht
  CHECK: git status --porcelain | wc -l
  EXPECT: 0
  EVIDENCE: pending
