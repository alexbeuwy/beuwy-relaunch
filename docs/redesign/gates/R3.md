# R3-Gates (Master-Ledger)

## A Fundament
- [ ] A2 Footer ohne „Ludwigshafen"
  CHECK: ! grep -q "Ludwigshafen" src/components/Footer.tsx
  EXPECT: exit 0
  EVIDENCE: pending
- [ ] A3 bw_-Schema liegt in Supabase (beuwy Funnels) + supabase/crm-schema.sql im Repo
  CHECK: test -f supabase/crm-schema.sql && grep -c "create table if not exists public.bw_" supabase/crm-schema.sql
  EXPECT: >=6
  EVIDENCE: pending

## B Welle 1
- [ ] B1 Rechner-Engine: node scripts/rechner-check.mjs → alle Tests grün
  CHECK: node scripts/rechner-check.mjs 2>&1 | tail -1
  EXPECT: enthält "ok"
  EVIDENCE: pending
- [ ] B2–B4 Tool-Routen antworten 200 und rendern Ergebniswerte clientseitig
  CHECK: for r in tools/verkaufspreisrechner tools/mietpreisrechner tools/afa-rechner; do curl -s -o /dev/null -w "%{http_code} " --noproxy localhost http://localhost:3100/$r; done
  EXPECT: 200 200 200
  EVIDENCE: pending
- [ ] B5/B6 Datenschutz deckt Funnel, Tools, Supabase, Resend, Bunny, Vercel, Cookies ab; Impressum vollständig strukturiert
  CHECK: grep -ci "supabase\|resend\|bunny\|vercel" src/app/datenschutz/page.tsx
  EXPECT: >=4
  EVIDENCE: pending
- [ ] B7 E-Mail-Sequenz: >=5 Vorlagen über emailLayout, Vorschau unter /intern/mails
  CHECK: grep -c "export function mail" src/lib/email-vorlagen.ts
  EXPECT: >=5
  EVIDENCE: pending
- [ ] B8 /intern: Login-geschützt, Lead-Pipeline aus bw_lead, Statuswechsel + Notizen funktionieren gegen Supabase
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/intern
  EXPECT: 200 (Login-Ansicht) 
  EVIDENCE: pending
- [ ] B9 /konto: Magic-Code-Login (bw_konto_code), Projektstatus/Tickets/Dokumente-Ansichten
  CHECK: curl -s -o /dev/null -w "%{http_code}" --noproxy localhost http://localhost:3100/konto
  EXPECT: 200
  EVIDENCE: pending
- [ ] B-Integration: Funnel-/Booking-Submits persistieren als bw_lead + lösen Bestätigungsmail-Vorlage aus (Demo-Modus ohne Key bleibt ehrlich)
  CHECK: grep -c "bw_lead\|crm/db" src/app/api/booking/route.ts
  EXPECT: >=1
  EVIDENCE: pending

## C Startseite als VSL
- [ ] C1 Blockreihenfolge folgt VSL-Faden (Hook→Problem→Mechanismus→Beweis→Einwände→Verknappung→CTA), fehlende Elemente ergänzt, Screenshot-Abnahme
  EVIDENCE: pending

## D 50 Seiten
- [ ] D1 50 neue Routen aus R3-SEITENPLAN.json existieren als page.tsx
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const fs=require('fs');console.log(p.seiten.filter(s=>fs.existsSync('src/app/'+s.route+'/page.tsx')).length)"
  EXPECT: 50
  EVIDENCE: pending
- [ ] D2 Jede der 50 Seiten: Metadata-Title mit Keyword, FAQPage-JSON-LD, erster Absatz beantwortet Suchfrage, interne Links laut Plan
  CHECK: node scripts/seitenplan-check.mjs
  EXPECT: "50/50 ok"
  EVIDENCE: pending
- [ ] D3 Integration: sitemap + llms.txt + /wissen-Hub führen alle 50, Footer-Wissen kuratiert
  CHECK: node -e "const p=require('./docs/redesign/R3-SEITENPLAN.json');const s=require('fs').readFileSync('src/app/sitemap.ts','utf8');console.log(p.seiten.filter(x=>s.includes(x.route)).length)"
  EXPECT: 50
  EVIDENCE: pending

## E Humanizer
- [ ] E1 Sweep-Protokoll docs/redesign/R3-HUMANIZER.md listet JEDE page.tsx mit Befund (geändert/sauber)
  EVIDENCE: pending

## F Verifikation
- [ ] F1 scripts/verify.mjs läuft komplett grün (Build, alle Routen 200, Greps)
  CHECK: node scripts/verify.mjs 2>&1 | tail -3
  EXPECT: "VERIFY: OK"
  EVIDENCE: pending
- [ ] F2 Alles committet und gepusht
  CHECK: git status --porcelain | wc -l
  EXPECT: 0
  EVIDENCE: pending
