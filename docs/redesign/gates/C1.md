# Gates C1 — /anfrage Vorquali-Funnel + Terminbuchung
- [x] G1 Mehrschritt-Funnel (3-4 Fragen: Rolle, Teamgroesse, Fokus, Zeithorizont), Fortschritt sichtbar, Motion-Tokens bei Schrittwechsel
      CHECK: ls src/app/anfrage/page.tsx src/components/AnfrageFunnel.tsx | wc -l
      EXPECT: 2
      EVIDENCE: `ls src/app/anfrage/page.tsx src/components/AnfrageFunnel.tsx | wc -l` → 2. Funnel hat 5 Schritte
      (Rolle/Größe/Fokus/Zeit/Kontakt), Fortschrittsleiste `role="progressbar"` mit
      `bg-akzent`-Füllung und `transition-[width] duration-[var(--duration-fast)]
      ease-[var(--ease-smooth-out)]` (AnfrageFunnel.tsx:210-213). Schrittwechsel über
      `AnfrageFunnel.module.css` (`.schritt`/`.vor`/`.zurueck`,
      `var(--duration-fast)`/`var(--ease-smooth-out)`/`var(--distance-large)`),
      reduced-motion-Pfad ohne Slide (module.css:29-35).
- [x] G2 Nach Quali: vereinfachte Terminbuchung (bestehendes BookingTool einbinden/verschlanken) ODER Kontaktdaten-Schritt mit /api/booking
      CHECK: grep -c "BookingTool\|/api/booking" src/components/AnfrageFunnel.tsx
      EXPECT: >=1
      EVIDENCE: `grep -c "BookingTool\|/api/booking" src/components/AnfrageFunnel.tsx` → 2.
      Eigener Kontaktdaten-Schritt (Name/E-Mail/Telefon/Freitext, Consent) postet an
      `POST /api/booking` (Vertrag der bestehenden Route gelesen, Route selbst nicht
      verändert) — die 4 Quali-Antworten wandern strukturiert ins `message`-Feld,
      `date`/`time` tragen ehrlich den Einreichungs-Zeitpunkt statt eines erfundenen
      Termin-Slots, da die Route dieses Format verlangt. 429/422/Netzwerkfehler mit
      konkreter Ansage, Eingabestand bleibt erhalten (kein Reset bei Fehler).
- [x] G3 noindex, robots-Meta gesetzt
      CHECK: grep -c "index: false" src/app/anfrage/page.tsx
      EXPECT: 1
      EVIDENCE: `grep -c "index: false" src/app/anfrage/page.tsx` → 1
      (`robots: { index: false, follow: false }`).
- [x] G4 Zustaende: laden/Fehler/Erfolg vorhanden, tsc gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: `npx tsc --noEmit 2>&1 | wc -l` → 0. Zustände: busy/"Wird gesendet…"
      (Button disabled), Fehler mit `role="alert"` + erhaltenem Formularstand,
      Erfolg mit Haken-Pop-Animation (`.hakenKreis`/`.hakenPfad`, Motion-Tokens
      `--duration-slow`/`--ease-bounce`/`--duration-medium`) + "Wir melden uns
      innerhalb von 24 Stunden" + Sekundär-Link zu `/cases`.
- [x] G5 Foto 12 als Seiten-Plate mit AiPille, Element-Bibliothek genutzt
      CHECK: grep -c "maklerAsset(12)" src/components/AnfrageFunnel.tsx src/app/anfrage/page.tsx 2>/dev/null | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: `grep -c "maklerAsset(12)" src/components/AnfrageFunnel.tsx
      src/app/anfrage/page.tsx | grep -v ":0" | wc -l` → 1 (page.tsx: 2 Treffer,
      mobile Bild-Bühne + Desktop-Plate). `<AiPille/>` auf beiden, `<GelbeKarte/>`
      aus `@/components/MaklerElemente` überlappt die linke Bildkante
      („Antwort innerhalb von 24 Stunden. Kein Pitch, keine Massenmail.").
