# Gates A2 — Helfer + Content-Keys
- [x] G1 src/lib/cdn.ts exportiert maklerAsset() + HERO_VIDEO
      CHECK: grep -c "maklerAsset\|HERO_VIDEO" src/lib/cdn.ts
      EXPECT: >=2
      EVIDENCE: grep cdn.ts = 3 (erwartet >=2)
- [x] G2 AiPille + VslSlot Komponenten existieren
      CHECK: ls src/components/AiPille.tsx src/components/VslSlot.tsx | wc -l
      EXPECT: 2
      EVIDENCE: ls = 2 Dateien (erwartet 2)
- [x] G3 Neue Content-Keys (hero.*, trust.*, stats.*) in content.ts, tsc gruen
      CHECK: npx tsc --noEmit 2>&1 | head -1 | wc -l
      EXPECT: 0
      EVIDENCE: tsc-Ausgabezeilen = 0 (erwartet 0)
