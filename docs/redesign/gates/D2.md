# Gates D2 — /leadgenerierung-immobilienmakler
- [x] G1 Seite existiert, XXL-Hero im System, VSL-Dramaturgie (Problem→Mechanismus→Beweis→CTA /anfrage)
      CHECK: ls src/app/leadgenerierung-immobilienmakler/page.tsx | wc -l
      EXPECT: 1
      EVIDENCE: Ausgabe = 1. Aufbau in der Datei: Hero (~70dvh, Foto 11, Floating Card,
      Highlight-Subline) → Problem (SektionsKopf + PainRows, 3 Absätze geteilte
      Kontakte/Preisvergleicher/kein Vertrauen) → Mechanismus (4-Stufen-Rail nach
      BRIEF §8-Tabelle, Feindbild "Lead-Portale") → GelbeKarte "Der Unterschied" →
      Beweis-Anriss (CaseGrid riegel-immobilien, 1 Ergebnis-Satz) → FAQ (3 Fragen,
      details/summary) → Finale-CTA (/anfrage).
- [x] G2 Title enthaelt "Leadgenerierung für Immobilienmakler", eigene Description, deutsch
      CHECK: grep -c "Leadgenerierung für Immobilienmakler" src/app/leadgenerierung-immobilienmakler/page.tsx
      EXPECT: >=1
      EVIDENCE: Ausgabe = 4 (metadata.title, openGraph.title, H1-Text, Kopfkommentar).
      metadata.title = "Leadgenerierung für Immobilienmakler: Eigentümer statt Kontakte |
      beuwy"; eigene description (Portal-Kontakt/Quelle-Framing), Sie-Form, deutsch.
- [x] G3 Interne Links: Hub + /anfrage + min. 1 Schwesterseite
      CHECK: grep -c "/anfrage\|/immobilienmarketing" src/app/leadgenerierung-immobilienmakler/page.tsx
      EXPECT: >=2
      EVIDENCE: Ausgabe = 2 (href="/anfrage" im gemeinsamen CTA-Button, href="/immobilienmarketing"
      im Finale-Fließtext). Zusätzlich href="/website-fuer-immobilienmakler" im selben
      Fließtext-Satz (Schwesterseite, per Leaf-Auftrag Punkt 7) — von diesem Grep-Muster
      nicht erfasst, im Quelltext aber vorhanden.
- [x] G4 Min. 1 Asset-Bild mit AiPille, tsc gruen
      CHECK: grep -c "AiPille\|maklerAsset" src/app/leadgenerierung-immobilienmakler/page.tsx
      EXPECT: >=1
      EVIDENCE: Ausgabe = 4 (Import + 2× AiPille-Nutzung/Referenz + maklerAsset(11)). tsc:
      `npx tsc --noEmit` liefert 2026-08-25 repo-weit genau 1 Fehler, in
      src/app/anfrage/page.tsx (fremdes, parallel laufendes Leaf C1 — Import von AiPille
      aus MaklerElemente, das dort keinen solchen Export hat). 0 Fehler stammen aus
      src/app/leadgenerierung-immobilienmakler/page.tsx (`tsc --noEmit 2>&1 | grep -c
      leadgenerierung-immobilienmakler` = 0). Diese Seite ist für sich tsc-grün; das
      Repo als Ganzes wird es erst nach Fix in C1s Datei.
- [x] G5 Foto 11 verbaut (AiPille) + Element-Bibliothek genutzt
      CHECK: grep -rc "maklerAsset(11)" src/app/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: Ausgabe = 1 (src/app/leadgenerierung-immobilienmakler/page.tsx:
      maklerAsset(11) im Hero, mit AiPille markiert). Element-Bibliothek genutzt:
      GelbeKarte, Highlight, SektionsKopf aus @/components/MaklerElemente; zusätzlich
      Reveal, PainRows, CaseGrid (bestehende, freigegebene Bausteine).
