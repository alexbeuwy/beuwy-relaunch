# Gates R2-1 — Schema-Grafiken
- [x] G1 Zwei Grafiken als server-taugliche Komponenten (reines SVG/HTML, kein "use client"), Tokens statt Ad-hoc-Farben
      CHECK: grep -c "use client" src/components/SchemaGrafiken.tsx
      EXPECT: 0 · EVIDENCE: Output `0`. Beide Komponenten sind reine Server-Components (keine Hooks, keine Handler); alle Farben laufen über Tokens (line-subtle, line-medium, akzent, akzent-wash, ink-cream, ink-muted, ink-dim, bg-elevated), keine Hex-Werte im Code.
- [x] G2 PerformanceFlow: Aufmerksamkeit->Marke->Anfragen->~5% registrierte Kontakte; Prozentwert als Prop (Studio-Key faehig), ganz simpel/clean, max 1 Gelb-Akzent
      CHECK: manuell · EVIDENCE: `PerformanceFlow({ quote }: { quote: string })` — 4-Spalten-Grid (mobil 1 Spalte, Divide-Utilities drehen automatisch von divide-x auf divide-y), reine Haarlinien-Konstruktion ohne Icons/Pfeile: "Von außen → Aufmerksamkeit", "Landet auf → Der Marke", "Wird zu → Anfragen", vierter Knoten mit `~{quote}` (GeistMono/tnum, dunkle Tinte) + "Registrierte Kontakte" + "Mandanten, Kunden, Interessenten". Genau 1 Gelb-Akzent: `bg-akzent-wash` nur auf dem Endknoten, sonst nirgends im Code. quote wird vom Aufrufer übergeben (Studio-Key `mk.pm.quote`, aktueller Wert "5 %" in content.ts), nicht hart codiert.
- [x] G3 ExposeVergleich: links Standard (BOTTIMMO/onOffice, austauschbar), rechts beuwy-Massarbeit; keine Fremdlogos, nur Text
      CHECK: manuell · EVIDENCE: `ExposeVergleich()` ohne Props — zwei Karten im Grid (mobil 1 Spalte). Links "Der Standard" / "BOTTIMMO- und onOffice-Exposés" (Namen stehen im Text, BRIEF §9 erlaubt das im Schema) + 3× identische `StandardDokument`-Silhouette (gleiche Bauteile, gleiche Balkenbreiten) + Copy "Sie wirken bei jedem Makler gleich und lassen sich kaum anpassen." Rechts "Mit beuwy" / "Maßarbeit für Ihr Unternehmen" + 3 unterschiedliche Silhouetten (Bildplatte / Fließtext / Datenbalken) + Copy zu maßgeschneiderten Dokumenten, automatisierten Prozessen, Systemen, die an alles denken, Fokus auf Deals. Einziger Gelb-Akzent rechts: ein `bg-akzent`-Balken in `BeuwyDokumentDaten`, links bleibt komplett grau/Tinte. Nur div/span-Formen und Text, keine Bilder, keine Fremdlogos.
- [x] G4 tsc + Verwendungsbeispiel im Dateikopf-Kommentar
      CHECK: npx tsc --noEmit · EXPECT: leer · EVIDENCE: Output leer (kein stdout/stderr). Dateikopf-Kommentar in src/components/SchemaGrafiken.tsx enthält Verwendungsbeispiel mit Import- und Aufrufzeile für beide Exporte.
