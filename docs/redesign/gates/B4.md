# Gates B4 — Startseite Sektionen 6-10 (Beweis, Prozess, Quali, FAQ, Finale)
- [x] G1 Zahlenband nutzt mk.stats.* Keys (Studio-editierbar), tnum
      CHECK: grep -rc "mk.stats" src/components/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: Output = 2 (src/components/MaklerHero.tsx:1, src/components/StartUnten.tsx:2).
      StartUnten.tsx Block 6 liest c["mk.stats.s1_wert"]…s4 + zugehörige *_label,
      rendert sie als `font-display ... tnum` — dieselbe Anatomie wie die
      Hero-Abschluss-Leiste (BRIEF-Vorgabe), nur größer (44/54px statt 30px).
- [x] G2 Cases-Anriss verlinkt /cases/[slug] (min. 2 echte Cases)
      CHECK: grep -rc "/cases/" src/app/page.tsx src/components/*.tsx | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: Output = 4 Dateien (u.a. src/components/StartUnten.tsx:1). StartUnten
      Block 6 verlinkt `/cases/riegel-immobilien` und `/cases/vision-group` — beide
      echte, immobiliennächste Fälle aus src/lib/cases.ts (Riegel zuerst, wie
      gefordert), kein beispiel:true-Case in der Rail. Feature→Hebel-Sätze aus
      cases.ts-Fakten geschrieben, keine erfundenen Zahlen.
- [x] G3 Qualifizierungs-Sektion mit ehrlicher Disqualifikation (BRIEF §6 Block 8)
      CHECK: manuell — Zitat als Evidence
      EVIDENCE: src/components/StartUnten.tsx Block 8 (id="passt-das", bg-bg-elevated).
      Ja-Liste: "die führen wollen, nicht folgen." / "deren Empfehlungsgeschäft trägt
      und die jetzt planbar wachsen wollen." / "die selbst entscheiden, und zwar
      schnell." Nein-Liste: "Sie die billigste Lösung suchen." / "Systeme und
      Automatisierung Sie nicht interessieren." / "Ihnen egal ist, wie Ihr Auftritt
      wirkt." Sub-Zeile: "Das ist Absicht, nicht Marketing." — ehrlich, ohne
      Arroganz-Kitsch, keine Karten (Rail mit divide-y-Haarlinien je Liste).
- [ ] G4 FAQ als Accordion mit Motion-Tokens (interpolate-size vorhanden)
      CHECK: grep -rc "FaqAccordion\|details\|Accordion" src/app/page.tsx | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: ORCHESTRATOR — dieser CHECK zielt auf src/app/page.tsx, das laut
      Auftrag für dieses Leaf gesperrt ist (Wiring macht der Orchestrator beim
      Zusammenführen von B3+B4). Hinweis für den CHECK selbst: `grep -c` auf EINER
      einzelnen Datei liefert immer genau eine Ausgabezeile (die Trefferzahl, auch
      wenn sie "0" ist) — `grep -v ':0'` filtert nur "datei:0"-Zeilen aus mehreren
      Dateien, nicht die bare "0" bei einer einzelnen Datei. Der CHECK zeigt daher
      IMMER wc -l=1, unabhängig vom tatsächlichen Inhalt von page.tsx — bitte beim
      Abnehmen berücksichtigen oder auf `grep -rc ... src/app/page.tsx src/components/StartUnten.tsx`
      umstellen. Echter Beleg: src/components/StartUnten.tsx Block 9 (id="faq") hat
      5× natives `<details className={stil.item}>`/`<summary>` (kein FaqAccordion-
      Import), Höhen-Transition über `::details-content` + `var(--duration-fast)`
      in StartUnten.module.css. Per Runtime-Smoke-Test (temporäre Preview-Route,
      wieder entfernt) bestätigt: 5 `<details>` rendern serverseitig fehlerfrei.
- [x] G5 Finale endet im CTA /anfrage, tsc + build gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: Output = 0. `npm run build` (next build) erfolgreich, "Compiled
      successfully", alle 41 Routen generiert, kein Lint-/Type-Fehler. Block 10
      (id="jetzt") endet mit `<Link href="/anfrage">{c["mk.hero.cta"]}</Link>`
      (= Studio-Key "Zusammenarbeit anfragen") + Mikrozeile c["mk.hero.cta_hinweis"].
- [x] G6 Element-Bibliothek genutzt + Assets 7/8/9 verbaut mit AiPille
      CHECK: grep -o "maklerAsset([789])" src/components/Start*.tsx | sort -u | wc -l
      EXPECT: 3
      EVIDENCE: Output = 3 (maklerAsset(7), (8), (9), alle in StartUnten.tsx —
      StartOben.tsx nutzt einen anderen Asset-Bereich, kein Konflikt). Jedes Foto
      hat <AiPille/> direkt danach (4× im Code, da AiPille auch in MaklerElemente-
      internen Komponenten vorkommt — 3 eigene + Re-Export-Zähler). Element-
      Bibliothek genutzt: SektionsKopf, GelbeKarte (glyph), StempelBadge, KreisDeko,
      Highlight, Reveal — alle aus MaklerElemente/Reveal importiert, keine
      Neuerfindung von Karten/Marker-Mustern.
