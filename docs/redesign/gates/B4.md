# Gates B4 — Startseite Sektionen 6-10 (Beweis, Prozess, Quali, FAQ, Finale)
- [ ] G1 Zahlenband nutzt mk.stats.* Keys (Studio-editierbar), tnum
      CHECK: grep -rc "mk.stats" src/components/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: pending
- [ ] G2 Cases-Anriss verlinkt /cases/[slug] (min. 2 echte Cases)
      CHECK: grep -rc "/cases/" src/app/page.tsx src/components/*.tsx | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: pending
- [ ] G3 Qualifizierungs-Sektion mit ehrlicher Disqualifikation (BRIEF §6 Block 8)
      CHECK: manuell — Zitat als Evidence
      EVIDENCE: pending
- [ ] G4 FAQ als Accordion mit Motion-Tokens (interpolate-size vorhanden)
      CHECK: grep -rc "FaqAccordion\|details\|Accordion" src/app/page.tsx | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: pending
- [ ] G5 Finale endet im CTA /anfrage, tsc + build gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: pending
