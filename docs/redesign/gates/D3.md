# Gates D3 — /website-fuer-immobilienmakler
- [x] G1 Seite existiert, XXL-Hero im System, VSL-Dramaturgie (Problem→Mechanismus→Beweis→CTA /anfrage)
      CHECK: ls src/app/website-fuer-immobilienmakler/page.tsx | wc -l
      EXPECT: 1
      EVIDENCE: `1` — Datei angelegt. Dramaturgie: Hero (Hook) → Spiegel (Problem, PainRows
      mit 3 rhetorischen Fragen zu Tempo/Exposés/Vertrauen) → 6 Editorial-Rails Feature→Hebel
      (Mechanismus, BRIEF §8 wörtlich) → Integrationen (Beweis/Kompatibilität) → Prozess
      (Einwand Zeit) → GelbeKarte Abgrenzung Baukasten → FAQ (Resteinwände) → Finale CTA.
- [x] G2 Title enthaelt "Website für Immobilienmakler", eigene Description, deutsch
      CHECK: grep -c "Website für Immobilienmakler" src/app/website-fuer-immobilienmakler/page.tsx
      EXPECT: >=1
      EVIDENCE: `5` — Metadata-Title exakt „Website für Immobilienmakler: Premium statt
      Baukasten | beuwy", eigene deutsche Description + OpenGraph, weitere Treffer im Fließtext
      (Hero-Eyebrow, H1, FAQ).
- [x] G3 Interne Links: Hub + /anfrage + min. 1 Schwesterseite
      CHECK: grep -c "/anfrage\|/immobilienmarketing" src/app/website-fuer-immobilienmakler/page.tsx
      EXPECT: >=2
      EVIDENCE: `4` — /anfrage (Hero-CTA + Finale-CTA) und /immobilienmarketing (Breadcrumb
      zum Hub + Finale-Textlink), zusätzlich /bottimmo-alternative, /maklerwebsite-kosten
      (GelbeKarte + FAQ) und /onoffice-website (Integrations-Sektion) als Schwesterseiten.
- [x] G4 Min. 1 Asset-Bild mit AiPille, tsc gruen
      CHECK: grep -c "AiPille\|maklerAsset" src/app/website-fuer-immobilienmakler/page.tsx
      EXPECT: >=1
      EVIDENCE: `4` — `<AiPille />` auf dem Hero-Foto, `maklerAsset(18)` importiert + im
      `<Image src=…>`. `npx tsc --noEmit` liefert für diese Datei und FaqAccordion.tsx keinen
      Treffer (`grep -i "website-fuer-immobilienmakler\|FaqAccordion" tsc-Output` → leer,
      exit 1). Repo-weit bleiben 2 Fehler in `src/app/anfrage/page.tsx` und
      `src/components/StartUnten.tsx` — beides fremde, parallel laufende Leaves (nicht
      D3-Scope, siehe Risiko im Abschlussbericht), nicht von diesem Leaf verursacht.
- [x] G5 Foto 18 verbaut (AiPille) + Element-Bibliothek genutzt
      CHECK: grep -rc "maklerAsset(18)" src/app/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: `1` — Foto 18 im Hero, mit AiPille. Element-Bibliothek aus MaklerElemente.tsx
      genutzt: SektionsKopf (4×), GelbeKarte, Highlight, StempelBadge, KreisDeko, LogoSlot +
      slugifyMarke (Integrationen), plus PainRows (Spiegel) und FaqAccordion (FAQ, Typ auf
      ReactNode erweitert für den Link in Frage 1).
