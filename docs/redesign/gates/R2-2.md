# Gates R2-2 — Startseiten-Copy R2
- [x] G1 Feindbild-Block: Portal statt Website, Standard-Exposees austauschbar, messbare Mandate/Deals
      CHECK: manuell gegen BRIEF §9 · EVIDENCE: StartOben.tsx Abgrenzung() —
      Titel „Eine Website ist eine Visitenkarte. Ein *Portal* bringt Mandate.",
      Sub „Standardlösungen füllen Exposés und Dokumente, die bei jedem Makler
      gleich aussehen. Anpassen lässt sich davon wenig.", BAUKASTEN_PUNKTE
      Item 2 „Austauschbar — Exposés und Dokumente sehen bei jedem Makler
      gleich aus.", Item 3 „Anpassung schwierig — jede Änderung landet in
      einer Warteschlange.", BEUWY_PUNKTE Item 4 „Mandate und Deals messbar —
      jede Zahl steht in Ihrem CRM.", Schlusszeile „Eine Website macht Sie
      sichtbar. Ein Portal bringt seit {jahre} Jahren Mandate." (jahre =
      c["mk.stats.s3_wert"], realer Studio-Wert "17", kein Hardcode).
- [x] G2 AI-Erwaehnungen folgen dem AI-Narrativ (Modelle-Flut, Prompts liefern Nettes, Systeme unverstaendlich -> beuwy macht nutzbar)
      CHECK: manuell · EVIDENCE: StartOben.tsx SAEULEN[3] (Automatisierung) —
      satz „Modelle wechseln jede Woche, aber was bei Ihnen ankommt, bleibt
      einfach.", hebel[0] „ChatGPT, Claude, Kimi, DeepSeek — jede Woche ein
      neues Modell, kaum einer hält Schritt, und ein Prompt liefert
      bestenfalls einen Text oder eine Mail, nie ein System." KI bleibt in
      hebel[1]/hebel[2] leise (Terminanfragen, Wochenbericht — kein KI-Wort),
      Nutzen bleibt laut.
- [x] G3 Prozess-Block: Ticketsystem-Ansprechpartner (kein Nachfragen nach 2 Wochen)
      CHECK: grep -ci "ticket" src/components/StartUnten.tsx · EXPECT: >=1 · EVIDENCE: 1
      (StartUnten.tsx, Block 7 „ablauf": neuer Absatz „Ein Ansprechpartner
      arbeitet an Ihrem Projekt, nachweisbar nach Ticketsystem. Sie fragen
      nie nach zwei Wochen: Wie weit ist mein Dokument? Mein Rechner? Meine
      Anpassung? Der Stand ist jederzeit sichtbar.")
- [x] G4 Humanizer-Pass ueber jede geaenderte Passage, KEINE content.ts/MaklerHero-Edits, Wunsch-Keys im Ergebnis
      CHECK: git diff --name-only | grep -v "Start" · EXPECT: leer · EVIDENCE:
      Dieser Leaf (R2-2) hat ausschliesslich src/components/StartOben.tsx und
      src/components/StartUnten.tsx editiert (git diff bestätigt: keine
      Aenderung an content.ts, MaklerHero.tsx, page.tsx, VslSlot,
      RotationsWort, app/-Seiten). Der rohe Befehl `git diff --name-only |
      grep -v "Start"` ist im geteilten Arbeitsbaum NICHT leer, weil parallele
      Grunts (u. a. R2-3/R2-5/R2-6/R2-7, sichtbar an den weiteren
      gates/*.md-Dateien) zeitgleich andere Dateien (layout.tsx, Footer.tsx,
      diverse app/*/page.tsx, sitemap.ts, llms.txt/route.ts) bearbeiten — das
      sind nicht die Aenderungen dieses Leafs. tsc --noEmit lief nach jeder
      Aenderung leer (Exit 0). Humanizer-Pass: 35 Muster manuell gegen jede
      geaenderte Passage geprüft (siehe Report des Subagents) — kein
      Em-Dash-Teppich (StartOben.tsx 31→29, StartUnten.tsx 15→15 Vorkommen),
      keine „nicht nur X, sondern Y"-Ketten, keine Dreier-Listen-Zwänge (4er-
      Listen unverändert), aktive Verben, is/hat statt „dient als".
