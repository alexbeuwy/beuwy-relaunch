# Gates E1 — /beste-maklerwebsites (Ranking-Asset)
- [x] G1 Seite: Methodik (Design, Ladezeit, Conversion-Pfad), 30 Slots, die grossen 8 namentlich, Rest "in Pruefung"
      CHECK: ls src/app/beste-maklerwebsites/page.tsx | wc -l
      EXPECT: 1
      EVIDENCE: Ausgabe = 1. Seite hat 5 Sektionen (Hero, Methodik, Liste, Ego-Loop, FAQ).
      Methodik: 3 Rails mit Icon (RiPaletteLine/RiTimerFlashLine/RiRouteLine), Gewichtung
      als t-stat (40 %/30 %/30 %), 2 Saetze + Messwerkzeug-Hinweis (mobile Ladezeitmessung,
      fester Kriterienkatalog). Liste: GROSSE_ACHT (8 benannte Zeilen, Wortmarke-Komponente,
      "Bundesweit", Score-Slots "–"/"Bewertung laeuft") + PLAETZE_9_BIS_30 (22× "Wird derzeit
      geprueft", 2-spaltiges Raster) = 30 Zeilen total.
- [x] G2 Scores/Bewertungstexte NICHT hart erfunden: Struktur + Studio-Keys bzw. neutrale Kriterien-Darstellung, Hinweis "Redaktionsstand"
      CHECK: manuell — wie geloest, als Evidence
      EVIDENCE: Kein einziger Score/Bewertungstext fuer ein echtes Unternehmen — alle 30
      Wertungs-Slots zeigen woertlich "–" mit Label "Bewertung laeuft" (grosse Acht) bzw.
      Name-Feld "Wird derzeit geprueft" (Plaetze 9–30, RangzeileKlein). RedaktionsPille
      "Redaktionsstand · August 2026" direkt ueber der Liste. Zusaetzlicher Disclaimer-Satz
      in der SektionsKopf-Sub: "Die Reihenfolge unten ist noch kein Urteil" — verhindert,
      dass die Positionierung der grossen Acht als verdeckte Qualitaetsaussage gelesen wird.
      FAQ 2 beantwortet "Kann man sich einkaufen?" explizit mit Nein. Keine Herabwuerdigung:
      kein negativer Satz ueber ein benanntes Haus irgendwo im Text.
      Texte laufen NICHT ueber src/lib/content.ts (Datei fuer diesen Leaf gesperrt) — daher
      hart im Code statt als Studio-Keys; Wunschliste dafuer im Leaf-Report.
- [x] G3 Ego-Loop-CTA: "Ihr Haus fehlt? Anfrage" -> /anfrage
      CHECK: grep -c "/anfrage" src/app/beste-maklerwebsites/page.tsx
      EXPECT: >=1
      EVIDENCE: Ausgabe = 2 (GelbeKarte-Primaer-CTA "Zusammenarbeit anfragen" + Sekundaer-CTA
      "Website pruefen lassen", beide -> /anfrage). Titel exakt "Ihr Haus fehlt in der Liste?"
      + Fliesstext exakt wie im Leaf-Auftrag vorgegeben.
- [x] G4 Title "Die 30 besten Maklerwebsites Deutschlands 2026", tsc gruen
      CHECK: grep -c "30 besten Maklerwebsites" src/app/beste-maklerwebsites/page.tsx
      EXPECT: >=1
      EVIDENCE: Ausgabe = 3 (metadata.title exakt "Die 30 besten Maklerwebsites Deutschlands
      2026 | beuwy", metadata.description, H1 rich()-String). npx tsc --noEmit: EIN Fehler
      projektweit, ausschliesslich in src/app/anfrage/page.tsx:4 (fremder, gleichzeitig
      laufender Leaf C1 — "AiPille" hat kein Re-Export aus MaklerElemente; von mir weder
      angelegt noch veraendert, git status zeigt die Datei als "??" von einem parallelen
      Agenten). beste-maklerwebsites/page.tsx selbst erzeugt keinen einzigen Fehler/Warning.
      Siehe Risiko im Leaf-Report — Fix liegt ausserhalb des E1-Scopes.
