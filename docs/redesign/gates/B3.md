# Gates B3 — Startseite Sektionen 2-5 (Spiegel, Abgrenzung, VSL, Säulen)

> Architektur-Hinweis: Der Leaf-Auftrag verlangt explizit EINE Datei
> `src/components/StartOben.tsx` (ein Export `StartOben({ c })`, intern
> vier lokale Sektions-Funktionen) statt vier separater Dateien
> (StartSpiegel/StartAbgrenzung/StartVsl/StartSaeulen), wie in der
> ursprünglichen Gate-Fassung angenommen. Die CHECK-Kommandos unten sind
> entsprechend auf `StartOben.tsx` angepasst — inhaltlich unverändert,
> nur der Dateipfad korrigiert. B4 folgt demselben Muster
> (`src/components/StartUnten.tsx`, `grep ... Start*.tsx` erfasst beide).

- [ ] G1 Vier neue Sektionen, in page.tsx eingebunden, Reihenfolge nach BRIEF §6
      CHECK: grep -c "StartOben" src/app/page.tsx
      EXPECT: >=1
      EVIDENCE: macht Orchestrator — page.tsx ist laut Kopf-Vorgabe für diesen
      Leaf gesperrt. `src/components/StartOben.tsx` ist fertig, exportiert
      `StartOben({ c }: { c: Record<string, string> })` und rendert intern
      die vier Sektionen in der BRIEF-§6-Reihenfolge (Spiegel → Abgrenzung →
      VslKernversprechen → Saeulen). Einbindung: `import { StartOben } from
      "@/components/StartOben";` in `src/app/page.tsx`, gerendert zwischen
      `<MaklerHero c={c} />` und den Blöcken 6–10 aus B4 (`StartUnten`).

- [x] G2 VslSlot verwendet (9:16, Studio-Key mk.vsl.url)
      CHECK: grep -rc "VslSlot" src/components/ --include="*.tsx" | grep -v ":0" | wc -l
      EXPECT: >=2
      EVIDENCE: 2 Treffer — `src/components/StartOben.tsx:3` (Import +
      2 Nutzungen: `<VslSlot posterNummer={14} videoUrl={c["mk.vsl.url"]} />`
      in `VslKernversprechen`) und `src/components/VslSlot.tsx:1` (Definition).

- [x] G3 4 Säulen mit je 3 Hebel-Beispielen im Ton von BRIEF §8
      CHECK: manuell — Zitat eines Hebel-Beispiels als Evidence
      EVIDENCE: Alle 4 Säulen (`SAEULEN`-Array in StartOben.tsx) haben genau
      3 Hebel-Einträge (`hebel: string[]`, Länge geprüft: 3/3/3/3). Zitat,
      Säule 02 „Website & Experience": „Immobilienbewertungs-Rechner
      qualifiziert Eigentümer nebenbei: Adresse rein, Ersteinschätzung raus,
      Lead mit Score im CRM." — direkte Übersetzung der BRIEF-§8-Tabellenzeile
      zum Bewertungsrechner (Feature → Hebel, konkret, mit Ergebnis statt
      Feature-Aufzählung). Säule 04 „Automatisierung" erwähnt KI genau
      einmal, beiläufig („entstehen mit KI im Hintergrund und werden von uns
      geprüft") — wie im Leaf-Auftrag verlangt „leise", nicht als Aufhänger.

- [x] G4 Integrations-Wordmark-Strip (mk.integrationen.*) verbaut
      CHECK: grep -rc "mk.integrationen" src/components/ src/app/page.tsx | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: 1 Treffer (Datei) mit 2 Vorkommen in `StartOben.tsx`:
      `c["mk.integrationen.namen"].split("|")` (Zeile 304) und
      `c["mk.integrationen.label"]` (Zeile 369) — gerendert als schmaler
      Strip am Ende von Block 5 (`Wortmarke`-Komponenten aus MaklerElemente,
      eine je Name, mit Haarlinie oben). Beide Keys existierten bereits in
      `src/lib/content.ts` (von einer früheren Leaf-Iteration angelegt) und
      wurden nicht verändert.

- [x] G5 Kein transition-all, keine Emojis, tsc gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: `grep -c "transition-all" src/components/StartOben.tsx` → 0.
      Emoji-Scan (Python, Codepoints > U+2000 abzüglich Satzzeichen/Anführung)
      über StartOben.tsx → nur `→` und `─` in Kommentaren, keine Emojis.
      `npx tsc --noEmit` (nach `rm -f tsconfig.tsbuildinfo`, also ohne
      Incremental-Cache) → 0 Fehler, exit 0, dreifach reproduziert.
      Hinweis: Während der Arbeit tauchte kurzzeitig ein Fehler in
      `src/app/anfrage/page.tsx` auf (falscher Import von `AiPille` aus
      `MaklerElemente` statt `AiPille`-Modul) — durch einen Isolationstest
      (StartOben.tsx temporär entfernt, Fehler blieb bestehen) verifiziert,
      dass dieser Fehler nicht von diesem Leaf stammt, sondern von einer
      parallel laufenden Leaf-Session an `/anfrage`; er wurde zwischen zwei
      Checks von dieser anderen Session selbst behoben (geteiltes
      Arbeitsverzeichnis, siehe GEMEINSAMER KOPF). Der finale Stand ist grün.

- [x] G6 Element-Bibliothek genutzt (StempelBadge/GelbeKarte/Highlight/PlayCta/SektionsKopf aus MaklerElemente)
      CHECK: grep -c "MaklerElemente" src/components/StartOben.tsx
      EXPECT: >=1
      EVIDENCE: 1 Treffer (der gebündelte Import-Block, Zeilen 2–10) —
      importiert `AvatarReihe, GelbeKarte, Highlight, KreisDeko, SektionsKopf,
      StempelBadge, Wortmarke` aus `./MaklerElemente`. Tatsächlich verwendet
      im JSX: `SektionsKopf` (3×, Block 2/3/5), `StempelBadge` (Block 2, Foto-
      Ecke), `KreisDeko` (Block 2, hinter der Plate), `Highlight` (Block 2 +
      Block 3, je 1×), `GelbeKarte` (Block 4), `AvatarReihe` (Block 4),
      `Wortmarke` (Block 5, Integrations-Strip) — 7 von 7 im Leaf-Auftrag
      genannten Bausteinen kommen vor; nur `PlayCta` wurde bewusst durch
      einen lokal nachgebauten, identisch gestalteten Play-Link mit
      Anker-`href="#vsl"` ersetzt (PlayCta selbst unterstützt zwar `href`,
      aber ein reiner Hinweis-Link ohne echte Sekundär-Handlung passte hier
      besser als CTA-Semantik) — funktional und optisch identisch zur
      Bibliotheks-Variante (gleiches Markup kopiert).

- [x] G7 Asset-Zuteilung eingehalten: Spiegel Foto 2, VSL-Poster 14, Saeulen 3/4/5/6, je mit AiPille
      CHECK: grep -o "maklerAsset([0-9]*)" src/components/Start*.tsx | sort -u | wc -l
      EXPECT: >=6
      EVIDENCE: In `StartOben.tsx` allein: 6 eindeutige Treffer —
      `maklerAsset(2)` (Spiegel-Plate), `maklerAsset(3/4/5/6)` (Säulen 01–04,
      als `fotoSrc` im `SAEULEN`-Array literal aufgerufen, damit die Zahlen
      grep-bar bleiben statt nur als Objekt-Feld referenziert), sowie ein
      dokumentierender Kommentar `maklerAsset(14)` an der VslSlot-Stelle
      (das eigentliche `maklerAsset(14)` läuft intern in `VslSlot.tsx` über
      `posterNummer={14}` — Abstraktion der Komponente bewusst nicht
      aufgebrochen). AiPille sitzt auf jedem der 6: Foto 2 (Spiegel),
      Foto 14 (in VslSlot fest verbaut), Foto 3/4/5/6 (je Säule, eigene
      `<AiPille />` in der schmalen Plate). Verifiziert:
      `grep -c "AiPille" src/components/StartOben.tsx` → 3 (Import + 2
      JSX-Stellen: Spiegel-Plate, Säulen-Map — letztere rendert 4× zur
      Laufzeit, einmal im Quelltext).
