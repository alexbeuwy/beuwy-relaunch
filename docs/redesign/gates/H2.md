# Gates H2 — Schlusspass
- [x] G1 transitions-polish-Durchlauf: keine Ad-hoc-Durations, Tokens ueberall
      CHECK: grep -rn "duration-\[0-9]\|duration: [0-9]" src/components src/app --include="*.tsx" | grep -v "var(--" | wc -l
      EXPECT: 0
      EVIDENCE: 0. Alle Redesign-module.css (MaklerHero, MaklerElemente,
      StartUnten, AnfrageFunnel, website-hero) laufen ueber
      var(--duration-*)/var(--ease-*); einzige Ausnahme die dokumentierte
      40s-Stempeldrehung. Verbliebene ms-Werte in globals.css sind
      Alt-Bestand von /check + BookingTool (bleibt laut Alex vorerst).
      Verwaiste SystemShowcase.module.css (Alt-One-Pager, nirgends mehr
      importiert) geloescht.
- [x] G2 make-interfaces-feel-better Quick-Review angewendet
      CHECK: manuell — 3 konkrete Verbesserungen als Evidence
      EVIDENCE: (1) Logo-Leisten flackern nie mehr als Broken-Image —
      Wortmarke steht sofort, SVG wechselt unsichtbar ein (LogoSlot).
      (2) Stempel-Badges ueberlappen jetzt tatsaechlich die Bildecken
      wie in der Referenz (Layering-Gefuehl statt Inline-Block).
      (3) Hub-Saeulen: ein Gelb-Akzent pro Viewport wiederhergestellt —
      die Sektion atmet, der CTA ist wieder der Fokus.
- [x] G3 npm run build + npx tsc gruen, keine neuen Konsolen-Errors beim Rendern
      CHECK: npm run build 2>&1 | grep -c "Compiled successfully"
      EXPECT: 1
      EVIDENCE: tsc --noEmit leer, next build 42/42 Routen gruen nach
      allen Fixes. Playwright-Konsolen-Check /termin: keine Errors nach
      Neustart (vorher main-app.js-404 durch stalen Server).
- [ ] G4 PR erstellt mit Screenshots
      CHECK: manuell — PR-URL als Evidence
      EVIDENCE: pending
