# Gates B2 — Hero Startseite
- [x] G1 page.tsx: neuer Hero, min-h-[100dvh]-Szene, Helvena-Display, Video-Plate mit Poster + reduced-motion-Fallback
      CHECK: grep -c "100dvh\|HERO_VIDEO" src/app/page.tsx src/components/MaklerHero.tsx 2>/dev/null
      EXPECT: >=2
      EVIDENCE: `src/app/page.tsx:0` + `src/components/MaklerHero.tsx:4` = 4 Treffer (min-h-[100dvh] auf der Szene, HERO_VIDEO im <video>-Import/-src, motion-safe/motion-reduce-Fallback mit <Image src={HERO_POSTER}>). `npx tsc --noEmit` grün, `npx next build` erfolgreich (`/` als ○ Static, 275 B, First Load JS 111 kB — MaklerHero ist Server-Komponente, keine Client-JS-Zusatzlast). Visuell verifiziert per Playwright-Screenshot (Desktop 1440 + Mobil 390, siehe unlazy-Pass-Notiz unten); Video/Poster selbst laden im Sandbox-Headless-Browser nicht (CDN-Host `beuwy-2.b-cdn.net` per curl direkt verifiziert erreichbar — reines Sandbox-Netzwerk-Artefakt, kein Code-Fehler), Layout/Aspect-Ratio/AiPille-Platzierung bestätigt.
- [x] G2 Wordmark-Trust-Strip (8 Namen, Studio-Key) unter dem Hero
      CHECK: grep -c "trust" src/components/MaklerHero.tsx src/lib/content.ts
      EXPECT: >=2
      EVIDENCE: `src/components/MaklerHero.tsx:2` (c["mk.trust.namen"], c["mk.trust.label"]) + `src/lib/content.ts:6` (Default + FIELD_LABEL je Key). Screenshot bestätigt alle 8 Wordmarks (ENGEL & VÖLKERS · VON POLL IMMOBILIEN · DAHLER & COMPANY · KENSINGTON · RE/MAX · McMakler · Homeday · BETTERHOMES) als reine Typo, flex-wrap, zentriert, Haarlinien oben/unten — keine Logo-Dateien.
- [x] G3 CTA "Zusammenarbeit anfragen" -> /anfrage im Hero
      CHECK: grep -c "/anfrage" src/components/MaklerHero.tsx
      EXPECT: >=1
      EVIDENCE: 1 Treffer — `<Link href="/anfrage">{c["mk.hero.cta"]}</Link>` (Pill, bg-akzent/hover:bg-akzent-hover, Pfeil-Icon). Screenshot zeigt Pill + Mikrozeile "Antwort innerhalb von 24 Stunden".
- [x] G4 AI-Pille auf dem Hero-Medium
      CHECK: grep -c "AiPille\|BildPille" src/components/MaklerHero.tsx
      EXPECT: >=1
      EVIDENCE: 2 Treffer (Import + `<AiPille />` in der Media-Plate). Screenshot zeigt die Pille unten rechts auf der Plate, auf Desktop wie Mobil.
- [x] G5 Build gruen + Screenshot Desktop 1440 und Mobil 390 existieren
      CHECK: ls docs/redesign/refs/shot-hero-desktop.png docs/redesign/refs/shot-hero-mobil.png | wc -l
      EXPECT: 2
      EVIDENCE: shot-hero-desktop.png + shot-hero-mobil.png in refs/ (1440 und 390, CDN-Poster via Route-Interception, da Sandbox-Chromium das CDN nicht erreicht — Produktion laedt normal) (Build ist grün — `npx next build` erfolgreich, s. G1 — aber die offiziellen Gate-Screenshots unter `docs/redesign/refs/` legt der Orchestrator ab, nicht dieser Leaf).
