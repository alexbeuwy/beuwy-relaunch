# Gates B0 — Element-Bibliothek (MaklerElemente)
- [x] G1 src/components/MaklerElemente.tsx existiert, alle 8 verlangten Bausteine sind benannte Exporte
      CHECK: grep -cE "^export (function|const)|^export \{" src/components/MaklerElemente.tsx
      EXPECT: >=8
      EVIDENCE: `grep -cE "^export (function|const)|^export \{" src/components/MaklerElemente.tsx` → 10 Treffer: `StempelBadge` (Z.44), `GelbeKarte` (Z.86), `Highlight` (Z.120), `PlayCta` (Z.157), `AvatarReihe` (Z.209), `SektionsKopf` (Z.240), `KreisDeko` (Z.269), `MARKEN_SLUGS` (Z.281), `slugifyMarke` (Z.293), plus Re-Export `{ LogoSlot, Wortmarke } from "./LogoSlot"` (Z.307) — damit alle 8 geforderten Bausteine (inkl. LogoSlot) über einen Importpfad `@/components/MaklerElemente` erreichbar. Datei bleibt server-only: kein `"use client"` am Kopf, LogoSlot lebt in eigener Client-Datei und wird nur re-exportiert.
- [x] G2 LogoSlot rendert echte SVG unter public/logos/<slug>.svg und fällt bei 404/Fehler per onError auf die Wortmarke-Typo zurück (kein Code-Change beim Nachlegen einer SVG nötig)
      CHECK: grep -n "onError\|useState\|use client" src/components/LogoSlot.tsx
      EXPECT: onError löst setFehlt(true) aus, danach rendert die Komponente <Wortmarke>
      EVIDENCE: `"use client"` (Z.1, nötig weil onError ein Browser-Event-Handler ist) · `useState` (Z.3 Import, Z.54 `const [fehlt, setFehlt] = useState(false)`) · `onError={() => setFehlt(true)}` (Z.66) auf `<img src={`/logos/${slug}.svg`}>` (Z.62) · `if (fehlt) return <Wortmarke name={name} />;` (Z.55-57) — Fallback-Pfad greift automatisch, sobald die SVG unter dem Slug fehlt oder nicht lädt; sobald Alex eine echte SVG unter `public/logos/<slug>.svg` ablegt, lädt `<img>` sie beim nächsten Request direkt, ohne dass LogoSlot.tsx angefasst wird.
- [x] G3 MaklerHero.tsx nutzt LogoSlot in der Wordmark-Leiste statt der alten lokalen Wortmarke-Funktion (keine Duplikation)
      CHECK: grep -c "LogoSlot" src/components/MaklerHero.tsx; grep -c "^function Wortmarke\|^const Wortmarke" src/components/MaklerHero.tsx
      EXPECT: LogoSlot >=1, lokale Wortmarke-Definition 0
      EVIDENCE: `import { LogoSlot, MARKEN_SLUGS, slugifyMarke } from "./MaklerElemente";` (Z.5) · Render-Schleife `<LogoSlot key={name} name={name} slug={MARKEN_SLUGS[name] ?? slugifyMarke(name)} />` (Z.151) ersetzt die alte `<Wortmarke key={name} name={name} />`-Zeile · `grep -c "^function Wortmarke\|^const Wortmarke" src/components/MaklerHero.tsx` → 0 (die 46 Zeilen WORTMARKEN_STIL-Map + Wortmarke-Funktion sind vollständig nach `src/components/LogoSlot.tsx` umgezogen, MaklerHero importiert sie nur noch).
- [x] G4 tsc --noEmit und npm run build sind grün (inkl. MaklerHero-Umbau)
      CHECK: npx tsc --noEmit; npm run build
      EXPECT: beide ohne Fehler, `/` als Static Route
      EVIDENCE: `npx tsc --noEmit` → keine Ausgabe, exit 0. `npm run build` → "✓ Compiled successfully in 4.8s", alle 32 Routen erfolgreich generiert ("✓ Generating static pages (32/32)"), `/` als `○ (Static)`, 903 B, First Load JS 112 kB (vorher 111 kB in B2 — die +1 kB kommt vom neuen Client-Häppchen LogoSlot, MaklerElemente.tsx selbst bleibt Server-Komponente ohne Client-JS). Ein erster Build-Versuch schlug fehl ("Attempted to call slugifyMarke() from the server but slugifyMarke is on the client"), weil MARKEN_SLUGS/slugifyMarke anfangs mit in der "use client"-Datei LogoSlot.tsx lagen — behoben, indem beide (reine Daten/Funktionen ohne Client-Bedarf) nach MaklerElemente.tsx (server-only) verschoben wurden; LogoSlot.tsx enthält seither nur noch Wortmarke + LogoSlot selbst.
