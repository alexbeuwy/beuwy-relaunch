# Gates D4 — /onoffice-website
- [x] G1 Seite existiert, XXL-Hero im System, VSL-Dramaturgie (Problem→Mechanismus→Beweis→CTA /anfrage)
      CHECK: ls src/app/onoffice-website/page.tsx | wc -l
      EXPECT: 1
      EVIDENCE: `1` — Datei existiert; Aufbau Hero → Problem (3 Absätze) → 4 Rails (Objekt-Sync/Anfragen/Rechner/Nachfassen)
      → GelbeKarte → Prozess (4 Wochen) + FAQ → Finale-CTA /anfrage, wie im Leaf-Auftrag verlangt.
- [x] G2 Title enthaelt "onOffice Website", eigene Description, deutsch
      CHECK: grep -c "onOffice Website" src/app/onoffice-website/page.tsx
      EXPECT: >=1
      EVIDENCE: `2` (title + openGraph.title) — "onOffice Website: Premium-Auftritt direkt am CRM | beuwy", eigene
      deutsche description in metadata.
- [x] G3 Interne Links: Hub + /anfrage + min. 1 Schwesterseite
      CHECK: grep -c "/anfrage\|/immobilienmarketing" src/app/onoffice-website/page.tsx
      EXPECT: >=2
      EVIDENCE: `4` — /anfrage (Hero-CTA + Finale-CTA), /immobilienmarketing (Breadcrumb + Finale-Link);
      zusätzlich /website-fuer-immobilienmakler (Finale) und /maklersoftware-vergleich (FAQ Q3).
- [x] G4 Min. 1 Asset-Bild mit AiPille, tsc gruen
      CHECK: grep -c "AiPille\|maklerAsset" src/app/onoffice-website/page.tsx
      EXPECT: >=1
      EVIDENCE: `4` — AiPille auf dem Hero-Foto gesetzt. `npx tsc --noEmit` liefert projektweit genau 1 Fehler,
      der aus `src/app/anfrage/page.tsx` (fremder, paralleler Leaf C1, ungetrackt) stammt — kein Treffer für
      "onoffice" im tsc-Output; diese Seite selbst ist tsc-sauber.
- [x] G5 Foto 13 verbaut (AiPille) + Element-Bibliothek genutzt
      CHECK: grep -rc "maklerAsset(13)" src/app/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: `1` (nur `src/app/onoffice-website/page.tsx` — asset-eindeutig laut GOAL-Zuteilung). Element-
      Bibliothek genutzt: SektionsKopf, GelbeKarte, Highlight, StempelBadge, KreisDeko, Reveal, AiPille, rich().
