# Gates G1 — Bestand
- [ ] G1 /cases-Uebersicht + Case-Seiten im neuen Look, Copy nach BRIEF §8 Tabelle
      CHECK: grep -rc "bg-akzent\|akzent" src/app/cases/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: pending
- [ ] G2 /termin bleibt erreichbar, verweist primaer auf /anfrage
      CHECK: grep -c "/anfrage" src/app/termin/page.tsx
      EXPECT: >=1
      EVIDENCE: pending
- [ ] G3 sitemap.ts: alle neuen Routen; robots: /anfrage + /api disallow-frei sauber
      CHECK: grep -c "immobilienmarketing\|beste-maklerwebsites\|maklerwebsite-kosten" src/app/sitemap.ts
      EXPECT: >=3
      EVIDENCE: pending
- [ ] G4 Alte Redirects in next.config.mjs geprueft/ergaenzt, tsc gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: pending
