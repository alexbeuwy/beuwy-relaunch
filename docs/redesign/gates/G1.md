# Gates G1 — Bestand
- [x] G1 /cases-Uebersicht + Case-Seiten im neuen Look, Copy nach BRIEF §8 Tabelle
      CHECK: grep -rc "bg-akzent\|akzent" src/app/cases/ | grep -v ":0" | wc -l
      EXPECT: >=1
      EVIDENCE: 2 (src/app/cases/page.tsx:1, src/app/cases/[slug]/page.tsx:1) —
        src/app/cases/page.tsx neu angelegt (Uebersicht, orderedCases() =
        Immobilien zuerst, GelbeKarte-CTA). src/app/cases/[slug]/page.tsx
        komplett restyled: section-band-bright/on-sky/text-sky/CtaBand raus,
        Ergebnis-Zahlen als .stat-band (Editorial, kein Kartengrid), riegel-
        immobilien.gebaut in src/lib/cases.ts auf Feature→Hebel-Ton nach
        BRIEF §8 umgeschrieben. tsc --noEmit: 0 Fehler, next build: 42/42
        Seiten gruen, Playwright-Screenshots von /cases, /cases/riegel-
        immobilien, /cases/koenigswege gegen Referenz-Analyse geprueft.
- [x] G2 /termin bleibt erreichbar, verweist primaer auf /anfrage
      CHECK: grep -c "/anfrage" src/app/termin/page.tsx
      EXPECT: >=1
      EVIDENCE: 4 — schlanker Hinweis-Kopf "Empfohlener Weg" verlinkt
        /anfrage vor dem H1, BookingTool unveraendert darunter weiter
        funktionsfaehig (Screenshot geprueft, Terminauswahl + Formular
        rendern normal).
- [x] G3 sitemap.ts: alle neuen Routen; robots: /anfrage + /api disallow-frei sauber
      CHECK: grep -c "immobilienmarketing\|beste-maklerwebsites\|maklerwebsite-kosten" src/app/sitemap.ts
      EXPECT: >=3
      EVIDENCE: 3 — sitemap.ts listet alle 9 neuen Routen + /cases +
        /cases/[jeder slug] (dynamisch aus CASES) + Bestand, /anfrage bewusst
        NICHT gelistet. robots.ts disallow: ["/api/", "/anfrage"]. Per
        `next build` + `curl` gegen /sitemap.xml und /robots.txt verifiziert
        (alle <loc> vorhanden, /anfrage fehlt in der Sitemap, robots.txt
        disallowed /api/ und /anfrage).
- [x] G4 Alte Redirects in next.config.mjs geprueft/ergaenzt, tsc gruen
      CHECK: npx tsc --noEmit 2>&1 | wc -l
      EXPECT: 0
      EVIDENCE: 0 — /anfrage-Redirect entfernt (war zuvor schon von
        anderem Leaf entfernt), /method→/immobilienmarketing,
        /system→/website-fuer-immobilienmakler, /manifesto→/, /work→/cases,
        /audit→/anfrage. Keine Kollision mit neuen Routen (keine der neuen
        Routen heisst /method|/system|/manifesto|/work|/audit). Per
        `next build` + `curl -I` auf allen 5 alten Pfaden verifiziert
        (307 auf die neuen Ziele). tsc --noEmit: 0 Fehler, next build:
        42/42 Seiten erfolgreich.
